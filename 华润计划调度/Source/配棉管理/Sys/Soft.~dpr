library Soft;

uses
  Forms,
  UBackUP in 'UBackUP.pas' {FrmBackUp},
  UDM in 'UDM.pas' {DM: TDataModule},
  URestore in 'URestore.pas' {FrmRestore},
  UPopedom in 'UPopedom.pas' {FrmPopedom},
  UPassword in 'UPassword.pas' {FrmPassword},
  UUser in 'UUser.pas' {FrmUser};

{$R *.res}
exports
  BackUp, Restore, Popedom, Password, User;
begin
  Application.Initialize;
  Application.CreateForm(TDM, DM);
  end.
